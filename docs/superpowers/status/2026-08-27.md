2026-08-27

- 继续昨天遗留的后端启动问题排查。
- 确认 `backend/app/main.py` 存在显性导入时 `NameError`：`frontend_dist` 在 `FrontendFallbackMiddleware` 使用前尚未定义，且 `/health` 路由重复定义。
- 已做最小修复：把 `frontend_dist` 定义提到中间件类之前，并删除重复的 `/health` handler。
- 复验方式：重新导入 `app.main` 并通过 `pytest`；未再出现 NameError。
