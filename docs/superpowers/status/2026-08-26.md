## 待继续任务（2026-08-26 留档）

- [ ] 精确定位并修复 `backend/tests/test_main.py` 中推荐相关用例的卡死/超时问题。
  - 已观察到：`tests/test_main.py` 单独执行时，至少 `test_get_candidate_routes_uses_fallback_when_amap_missing` 会超时；相同逻辑在 `tests/test_route_engine.py` 与 `tests/test_poi_explorer.py` 中可正常快速完成。
  - 下一步需要继续缩小范围：确认是测试 fixture/网络/路径导入/断言错误导致的挂起，再做最小修复。
- [ ] 修复后补跑完整后端测试，并更新当前验证结果。

## 备注

- 前端单测目前正常：`10 passed`。
- 前端已进入“继续优化单调感”阶段，但本次因后端测试阻塞先停在这一步，等待继续。
